/**
 * Брейкпоинты для адаптивного дизайна
 *
 * ⚠️ ВАЖНО: При изменении значения нужно также обновить все @media (max-width: 767px) в CSS файлах!
 * CSS переменные нельзя использовать в медиа-запросах, поэтому используется хардкод.
 *
 * Для поиска всех использований: grep -r "767px" frontend/src --include="*.css"
 */
export const BREAKPOINTS = {
  mobile: 767, // в пикселях
} as const;
